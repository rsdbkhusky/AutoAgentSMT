
- user：
你需要为我写一个提示词，这个提示词将会交于另一个负责编写项目的agent作为其初始提示词。
我希望让这个agent做为一位AI工程师，让他接下来完成一个多Agent虚拟股市交易模拟的项目，也就是人工定义一个虚拟的股市，给定交易规则、各支股票的初始状态等等其他所有需要定义的东西，一些agent有不同的人物背景、性格、权势、本金等等，以及其他需要用到的东西。你需要写在提示词里的内容包含“项目工程规范”、“项目内容细节”等等，根据你的理解进行补充

- user：
你的提示词整体没有大问题，但是有以下几个问题，在以上提示词的基础上修改：
1. 修改“项目工程规范-模块化设计”的表述，我要求的是解耦解耦再解耦，分层分层再分层，由于之后我的要求会大幅更改，你需要大大地解耦，封装为大量有独立逻辑的不同层次的类，例如LLM类有base_url,api_key,model等属性，有调用的方法，作为单个llm；以及一些Agent类，他们可以有不同的toolcall,memory,架构和工作逻辑，可以作为单个Agent。分层指的是分为：
    a. 前端层：<工具待定>，最终成品为一个前端网页，负责显示包括(i)一个类似同花顺的交易界面并且可以切换各个单股、指数、分时、日K等 (ii)聊天界面，可以有若干股市交流群聊 (iii)新闻界面，模拟新闻 (iiii)其他你认为需要的
    b. 后端层：FastAPI：负责API路由，数据验证，业务逻辑等
    c. 智能体层：一系列搭建智能体层的python库：负责搭建Agent
    d. 外部服务层：调用LLMAPI、调用RAG等等其他工具调用等等
2.添加“项目工程规范-模块化设计”：给出项目目录结构
3.按照我的以上要求，再补充一些内容，例如给出整个项目的一个细致入微的运行流程实例，来对齐写代码的agent的认知和我的设计的一致性

- gemini：

### 1. 角色定义

你是一位拥有深厚系统架构经验的 **AI 软件工程师**。你擅长使用 **领域驱动设计 (DDD)** 和 **面向对象原则** 构建极其复杂、高扩展性的系统。你现在的任务是设计并实现一个“多 Agent 虚拟股市交易模拟系统”。该系统必须做到**模块化、原子化、高度解耦**，以应对未来大幅度的逻辑变更。

---

### 2. 项目工程规范：深度解耦与分层架构

你必须严格按照以下四层模型进行架构设计，每一层仅通过标准 API 或接口进行通信：

* **A. 前端层 (Frontend)**：基于现代 Web 框架（如 React/Next.js）。
* **交易终端**：高仿“同花顺”界面，包含多股切换、K 线图（分时/日K）、实时盘口。
* **社交系统**：模拟股市交流群，Agent可以自动在群内对话，展示不同 Agent 的观点碰撞。
* **资讯中心**：滚动播放影响市场的宏观与行业新闻，Agent可以模拟获取这些内容。


* **B. 后端层 (Backend - FastAPI)**：系统的指挥中心。
* 负责 RESTful API 路由、用户/Agent 身份验证、业务逻辑调度及 WebSocket 实时数据推送。


* **C. 智能体层 (Agentic Layer)**：基于 Python 类库的高度封装。
* **原子级 LLM 类**：封装 `base_url`, `api_key`, `model`。支持多种 Provider 切换。
* **多样化 Agent 类**：每个 Agent 是独立的类实例，拥有专属的 `Memory`（短期/长期）、`ToolCall` 集合、`Framework`（如 ReAct 或规划逻辑）及性格参数。


* **D. 外部服务层 (External Services)**：
* LLM API 调用、RAG 向量数据库对接、外部数据爬虫或模拟数据生成工具。

如果有你认为需要调整和补充的地方，告知我

---

### 3. 项目目录结构 (Project Layout)

为了保证解耦，请遵循以下目录规范：

```text
/26031200project（目前项目根文件夹）
├── /src 				   # 所有代码都需放在该目录下
│	├── /frontend          # 前端代码 (React/TypeScript)
│	├── /backend           # FastAPI 后端核心，业务逻辑等
│	├── /agents            # 智能体层
│	├── /services          # 外部服务集成 (RAG, Web Search...)
│	└── main.py
└── /config				   # 所有配置文件，我要求main.py的参数只有一个--config，传参主配置文件路径，并且将所有可以条件的内容都放在配置文件中
```

关于项目结构、文件目录、代码文件的设计，你需要满足“在完美完成我所有要求的前提下尽量减少项目结构、代码的阅读复杂度”的要求，简练设计，不要使用__init__等。并且注释、输出和页面均使用中文

更加具体的第三级目录设计需要你自己按照需要设计

如果有你认为需要调整和补充的地方，告知我

---

### 4. 项目运行流程实例 (Step-by-Step Logic)

为了对齐认知，系统的一个典型运行周期如下：

1. **环境初始化**：`Backend` 启动，加载 `MarketEngine`，初始化股票代码及初始价格。
2. **Agent 激活**：`Agentic Layer` 根据配置实例化 50 个具有不同性格（如：贪婪、稳健、跟风）的 Agent。
3. **新闻触发**：`External Services` 生成若干新闻，例如“某科技大厂财报不及预期”的新闻。
4. **感知与决策**：
* `Backend` 通过消息队列通知相关 Agent。
* Agent 调用其 `LLM类` 结合自身 `Memory` 和 `ToolCall` 分析新闻对持仓的影响。
* Agent 决定操作方案。


5. **订单提交**：Agent 通过 `FastAPI` 接口提交交易订单。
6. **撮合与反馈**：`MarketEngine` 执行撮合，更新股价，并将结果通过 `WebSocket` 推送至 `Frontend`。
7. **社交互动**：Agent 在“交流群”模块发表看法，可能诱发其他散户 Agent 的追涨杀跌。

---

### 5. 任务指令

1. **定义接口协议**：首先输出 Agent 层与 Backend 层交互的 API 契约（OpenAPI 规范）。
2. **类层级设计**：定义 `BaseLLM` 和 `BaseAgent` 的抽象接口，展示如何通过继承实现不同的 Agent 逻辑。
3. **核心撮合逻辑**：编写不依赖任何特定 Agent 的独立股市撮合类。
4. **逐步实现**：在后续对话中，我将要求你逐个模块编写代码，请随时准备好进行深度的模块拆解。

---

现在是整个项目的冷启动阶段，你需要先阅读这个目录，仔细理解我的要求，试着提出一些潜在的问题并尝试解决，然后进入plan模式仔细给出完整方案（包括完整项目目录、各个文件内容），你要拟定的是第一个方案，第一个方案不要求过于充分地实现所有逻辑，要求实现最基础的前端后端和智能体代码，以及能完整运行整个流程的main.py，然后给出完整运行的命令（注意你不要直接运行，要让我运行），先按照这些要求进行plan




- user：
解释方案中的以下内容，解释我问你的问题，并且按照我的要求调整方案：
1. 你提到“4种性格子类（保守/激进/跟风/价值）”，当前方案是否保证这些性格只和提示词有关，并不是每个性格就有一个子类，为什么你认为每个性格都应该有子类？应该是性格可以任意拟定，数量可以任意，甚至每个agent本身就是一个配置文件，其提示词也在配置文件中，main.py使用的主配置文件应该是config/main.yaml而非default.yaml，在主配置文件中定义50个agent方式是使用一个列表，列表中每个元素是一个指向关于agent的子配置文件的路径，例如有四种agent（包括不同财富、地位、消息渠道、性格等），就是config/agent/agent_xxx.yaml以及agent_yyy,agent_zzz,agent_www，main.yaml中的agent配置文件列表的每个元素是指向这些子配置文件的路径
2. 关于前端，我要的前端并不是一个页面就显示交易终端，群聊，资讯，而是希望你写一个简易的类似windows底部任务栏这样的东西，可以切换三者
3. 虚拟市场有虚拟时间轴，你需要模拟时间，例如模拟小时模拟日，虚拟时间可以是类似2100年1月1日这样的但是不能没有时间
4. 我不用__init__的原因是这个项目不需要打包成库，为什么main.py 需要将 src/ 加入 sys.path？
5. Agent数量，股票数量、股票名称和其他所有股票的属性等等都是配置的参数，可以配置。默认参数是Agent数量50，股票数量30
6. 你需要注意一点，虽然新闻是生成的，但是整个股票的交易，包括K线变化等等都是完全根据Agent的真实交易和交易规则模拟出来的，不是由另一套逻辑生成的




- user：
解释方案中的以下内容，解释我问你的问题，并且按照我的要求调整方案：
1. 你疑似有一点错误认知：并不是每个Agent就必须有一个子配置文件，而是每种Agent一个子配置文件，例如可以有多个同一种的Agent被实例化




- user：
仔细检查整个方案，试着自己提出一些潜在的问题并且试着解决，仔细分析所有我的要求，如果没有其他问题，则开始实现